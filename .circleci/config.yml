version: 2.1

workflows:
  build-workflow:
    jobs:
      - runner
jobs:
  runner:
    machine: true
    resource_class: mrerenk/aosp
    steps:
      - run: sudo apt update -yqq && sudo apt install -y python3-pip
